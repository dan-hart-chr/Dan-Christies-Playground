# Fonts

This folder stores shared webfont assets used by the design system. Many projects keep copies of the Arizona font files in their own `public/fonts` directories; this location provides a central place for shared font CSS and licenses.

See the `arizona/` subfolder for the Arizona font registrations and guidance.
